#ifndef __PRIMES_H__
#define __PRIMES_H__

#include <stdbool.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

// Returns a count of the number of divisiors for 'number'.
size_t divisorCount(size_t number);


// Generates a new prime number on each call.  If 'reset' is true
// reset generator back to first prime.
//size_t nextPrime(bool reset);


// Return true if number is prime
//bool isPrime(size_t n);

#endif

